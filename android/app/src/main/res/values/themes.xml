<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.M5Scribe" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Status bar color -->
        <item name="android:statusBarColor">@color/status_bar_blue</item>
        <!-- Dark icons on light status bar -->
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.M5Scribe" parent="Base.Theme.M5Scribe" />
</resources>